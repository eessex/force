version: "2.4"
services:
  force:
    command: scripts/acceptance.sh src/test/acceptance/*.js
    extends:
      file: build.yml
      service: force
    environment:
      - CI=true
      - CIRCLE_NODE_INDEX
    env_file: ../.env.test
